import { z } from "zod";
import { 
// SzUnknown,
// NUMBER_FORMATS,
STRING_KINDS, } from "./types.js";
export const PRIMITIVES = {
    ZodString: "string",
    ZodNumber: "number",
    ZodBoolean: "boolean",
    ZodNaN: "nan",
    ZodBigInt: "bigInt",
    ZodDate: "date",
    ZodUndefined: "undefined",
    ZodNull: "null",
    ZodAny: "any",
    ZodUnknown: "unknown",
    ZodNever: "never",
    ZodVoid: "void",
    ZodSymbol: "symbol",
};
const getCustomChecksAndErrors = (def, opts) => {
    let customChecks = null;
    if ("checks" in opts && opts.checks) {
        customChecks = def.checks
            ?.filter((check) => {
            const chk = check._zod.def.check;
            return chk == "custom";
        })
            .map((check) => {
            const name = Object.entries(
            /* c8 ignore next -- TS doesn't catch */
            opts.checks || {}).find(([, checkFunc]) => {
                return checkFunc === check._zod.check;
            })?.[0];
            if (name) {
                return { name };
            }
            // May be a refinement
            return null;
        })
            .filter(Boolean);
    }
    let customError;
    if ("error" in def) {
        const key = Object.entries(opts.errors ?? {}).find(([, func]) => {
            return func === def.error;
        })?.[0];
        customError =
            typeof key == "string"
                ? { key }
                : // Not supplying an issue should not be a problem for regular
                    //   wrapped string errors
                    def.error();
    }
    return Object.assign(customChecks ? { checks: customChecks } : {}, customError ? { error: customError } : {});
};
const s = zerializeRefs;
const zerializers = {
    optional: (def, opts) => ({
        ...s(def.innerType, opts, true),
        isOptional: true,
    }),
    nullable: (def, opts) => ({
        ...s(def.innerType, opts, true),
        isNullable: true,
    }),
    default: (def, opts) => ({
        ...s(def.innerType, opts, true),
        defaultValue: def.innerType._zod.def.type === "bigint"
            ? String(def.defaultValue)
            : def.innerType._zod.def.type === "date"
                ? def.defaultValue.getTime()
                : def.defaultValue,
    }),
    number: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            const format = check._zod.def.format;
            return {
                ...o,
                ...(chk == "greater_than"
                    ? {
                        min: check._zod.def.value,
                        ...(check._zod.def.inclusive
                            ? { minInclusive: true }
                            : {}),
                    }
                    : chk == "less_than"
                        ? {
                            max: check._zod.def
                                .value,
                            ...(check._zod.def
                                .inclusive
                                ? { maxInclusive: true }
                                : {}),
                        }
                        : chk == "multiple_of"
                            ? {
                                multipleOf: check._zod
                                    .def.value,
                                /* c8 ignore next 2 -- Guard */
                            }
                            : {}),
            };
        }, {});
        return Object.assign({
            type: "number",
            ...checks,
            ...getCustomChecksAndErrors(def, opts),
            // Check is on `def` itself
            ...("check" in def && def.check === "number_format" && "format" in def
                ? { format: def.format }
                : {}),
        }, def.coerce ? { coerce: true } : {});
    },
    template_literal: (def, opts) => {
        const parts = def.parts.map((part, idx) => {
            if (typeof part == "string") {
                return part;
            }
            return s(part, {
                ...opts,
                currentPath: [...opts.currentPath, "parts", String(idx)],
            });
        });
        return {
            type: "templateLiteral",
            parts,
            ...(def.format ? { format: def.format } : {}),
        };
    },
    string: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            const format = check._zod.def.format;
            return {
                ...o,
                ...(chk == "min_length"
                    ? { min: check._zod.def.minimum }
                    : chk == "max_length"
                        ? { max: check._zod.def.maximum }
                        : chk == "length_equals"
                            ? {
                                length: check._zod.def
                                    .length,
                            }
                            : // Any way around this?
                                chk === "overwrite" &&
                                    check._zod.def.tx.toString() ===
                                        "(input) => input.toUpperCase()"
                                    ? { toUpperCase: true }
                                    : chk === "overwrite" &&
                                        check._zod.def.tx.toString() ===
                                            "(input) => input.toLowerCase()"
                                        ? { toLowerCase: true }
                                        : chk === "overwrite" &&
                                            check._zod.def.tx.toString() === "(input) => input.trim()"
                                            ? { trim: true }
                                            : // No apparent check
                                                // : chk == "string_format" && format == "trim"
                                                // ? { trim: true }
                                                chk == "string_format" && format == "starts_with"
                                                    ? {
                                                        startsWith: check._zod
                                                            .def.prefix,
                                                    }
                                                    : chk == "string_format" && format == "ends_with"
                                                        ? {
                                                            endsWith: check._zod
                                                                .def.suffix,
                                                        }
                                                        : chk == "string_format" && format == "includes"
                                                            ? {
                                                                includes: check._zod
                                                                    .def.includes,
                                                                position: check._zod
                                                                    .def.position,
                                                            }
                                                            : chk == "string_format" && format == "regex"
                                                                ? {
                                                                    regex: check._zod.def
                                                                        .pattern.source,
                                                                    ...(check._zod.def
                                                                        .pattern.flags
                                                                        ? {
                                                                            flags: check
                                                                                ._zod.def.pattern.flags,
                                                                        }
                                                                        : {}),
                                                                    /* c8 ignore next 2 -- Guard */
                                                                }
                                                                : {}),
            };
        }, {});
        const format = "format" in def && def.format;
        return Object.assign({
            type: "string",
            ...checks,
            ...getCustomChecksAndErrors(def, opts),
            // Check is on `def` itself
            ...(format == "ipv4"
                ? { kind: "ip", version: "v4" }
                : format == "ipv6"
                    ? { kind: "ip", version: "v6" }
                    : format == "cidrv4"
                        ? { kind: "cidr", version: "v4" }
                        : format == "cidrv6"
                            ? { kind: "cidr", version: "v6" }
                            : format == "uuid"
                                ? {
                                    kind: "uuid",
                                    ...("version" in def ? { version: def.version } : {}),
                                }
                                : format == "jwt"
                                    ? {
                                        kind: "jwt",
                                        ...("alg" in def ? { algorithm: def.alg } : {}),
                                    }
                                    : format == "email"
                                        ? {
                                            kind: "email",
                                            ...("pattern" in def &&
                                                def.pattern &&
                                                typeof def.pattern == "object" &&
                                                "source" in def.pattern &&
                                                "flags" in def.pattern &&
                                                def.pattern.source !== z.regexes.email.source
                                                ? {
                                                    pattern: def.pattern.source,
                                                    flags: def.pattern.flags,
                                                }
                                                : {}),
                                        }
                                        : ("format" in def && STRING_KINDS.has(format)) ||
                                            format == "datetime" ||
                                            format == "time"
                                            ? {
                                                kind: format,
                                                ...("precision" in def && def.precision
                                                    ? {
                                                        precision: def.precision,
                                                    }
                                                    : {}),
                                                ...("offset" in def && def.offset
                                                    ? {
                                                        offset: def.offset,
                                                    }
                                                    : {}),
                                                ...("local" in def && def.local
                                                    ? {
                                                        local: def.local,
                                                    }
                                                    : {}),
                                            }
                                            : {}),
        }, def.coerce ? { coerce: true } : {});
    },
    boolean: (def) => Object.assign({ type: "boolean" }, def.coerce ? { coerce: true } : {}),
    nan: () => ({ type: "nan" }),
    symbol: () => ({ type: "symbol" }),
    bigint: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            return {
                ...o,
                ...(chk == "greater_than"
                    ? {
                        min: String(check._zod.def.value),
                        ...(check._zod.def.inclusive
                            ? { minInclusive: true }
                            : {}),
                    }
                    : chk == "less_than"
                        ? {
                            max: String(check._zod.def.value),
                            ...(check._zod.def
                                .inclusive
                                ? { maxInclusive: true }
                                : {}),
                        }
                        : chk == "multiple_of"
                            ? {
                                multipleOf: String(check._zod.def
                                    .value),
                                /* c8 ignore next 2 -- Guard */
                            }
                            : {}),
            };
        }, {});
        return Object.assign({
            type: "bigInt",
            ...checks,
            ...getCustomChecksAndErrors(def, opts),
            // Check is on `def` itself
            ...("check" in def && def.check === "bigint_format" && "format" in def
                ? { format: def.format }
                : {}),
        }, def.coerce ? { coerce: true } : {});
    },
    file: (def) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            return {
                ...o,
                ...(chk == "min_size"
                    ? {
                        min: check._zod.def.minimum,
                    }
                    : chk == "max_size"
                        ? {
                            max: check._zod.def.maximum,
                        }
                        : chk == "mime_type"
                            ? {
                                mime: check._zod.def.mime,
                                /* c8 ignore next 2 -- Guard */
                            }
                            : {}),
            };
        }, {});
        return {
            type: "file",
            ...checks,
        };
    },
    date: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            return {
                ...o,
                ...(chk == "greater_than"
                    ? {
                        min: check._zod.def.value.getTime(),
                        // ...((check as z.core.$ZodCheckLessThan<Date>)._zod.def.inclusive
                        //   ? { minInclusive: true }
                        //   : {}),
                    }
                    : chk == "less_than"
                        ? {
                            max: check._zod.def
                                .value.getTime(),
                            // ...((check as z.core.$ZodCheckLessThan<Date>)._zod.def.inclusive
                            //   ? { maxInclusive: true }
                            //   : {}),
                        }
                        : /* c8 ignore next -- Guard */
                            {}),
            };
        }, {});
        return Object.assign({ type: "date", ...checks, ...getCustomChecksAndErrors(def, opts) }, def.coerce ? { coerce: true } : {});
    },
    undefined: () => ({ type: "undefined" }),
    null: () => ({ type: "null" }),
    any: () => ({ type: "any" }),
    unknown: () => ({ type: "unknown" }),
    never: () => ({ type: "never" }),
    void: () => ({ type: "void" }),
    literal: (def) => ({ type: "literal", values: def.values }),
    tuple: (def, opts) => ({
        type: "tuple",
        ...getCustomChecksAndErrors(def, opts),
        items: def.items.map((item, idx) => {
            const result = s(item, {
                ...opts,
                currentPath: [...opts.currentPath, "items", String(idx)],
            });
            return result;
        }),
        ...(def.rest
            ? {
                rest: s(def.rest, {
                    ...opts,
                    currentPath: [...opts.currentPath, "rest"],
                }),
            }
            : {}),
    }),
    set: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            return {
                ...o,
                ...(chk == "min_size"
                    ? {
                        minSize: check._zod.def.minimum,
                    }
                    : chk == "max_size"
                        ? {
                            maxSize: check._zod.def.maximum,
                        }
                        : chk == "size_equals"
                            ? {
                                minSize: check._zod.def.size,
                                maxSize: check._zod.def.size,
                                /* c8 ignore next 2 -- Guard */
                            }
                            : {}),
            };
        }, {});
        return {
            type: "set",
            value: s(def.valueType, {
                ...opts,
                currentPath: [...opts.currentPath, "value"],
            }),
            ...getCustomChecksAndErrors(def, opts),
            ...checks,
        };
    },
    array: (def, opts) => {
        const checks = def.checks?.reduce((o, check) => {
            const chk = check._zod.def.check;
            return {
                ...o,
                ...(chk == "min_length"
                    ? { minLength: check._zod.def.minimum }
                    : chk == "max_length"
                        ? {
                            maxLength: check._zod.def
                                .maximum,
                        }
                        : chk == "length_equals"
                            ? {
                                minLength: check._zod.def
                                    .length,
                                maxLength: check._zod.def
                                    .length,
                                /* c8 ignore next 2 -- Guard */
                            }
                            : {}),
            };
        }, {});
        return {
            type: "array",
            element: s(def.element, {
                ...opts,
                currentPath: [...opts.currentPath, "element"],
            }),
            ...getCustomChecksAndErrors(def, opts),
            ...checks,
        };
    },
    object: (def, opts) => {
        return {
            type: "object",
            ...getCustomChecksAndErrors(def, opts),
            ...(!def.catchall
                ? {}
                : {
                    catchall: s(def.catchall, {
                        ...opts,
                        currentPath: [...opts.currentPath, "catchall"],
                    }),
                }),
            properties: Object.fromEntries(Object.entries(def.shape).map(([key, schema]) => [
                key,
                s(schema, {
                    ...opts,
                    currentPath: [...opts.currentPath, "properties", key],
                }),
            ])),
        };
    },
    record: (def, opts) => ({
        type: "record",
        ...getCustomChecksAndErrors(def, opts),
        key: s(def.keyType, {
            ...opts,
            currentPath: [...opts.currentPath, "key"],
        }),
        value: s(def.valueType, {
            ...opts,
            currentPath: [...opts.currentPath, "value"],
        }),
    }),
    map: (def, opts) => ({
        type: "map",
        ...getCustomChecksAndErrors(def, opts),
        key: s(def.keyType, {
            ...opts,
            currentPath: [...opts.currentPath, "key"],
        }),
        value: s(def.valueType, {
            ...opts,
            currentPath: [...opts.currentPath, "value"],
        }),
    }),
    enum: (def) => ({ type: "enum", values: def.entries }),
    union: (def, opts) => {
        return {
            type: "discriminator" in def ? "discriminatedUnion" : "union",
            ...("discriminator" in def
                ? {
                    discriminator: def.discriminator,
                }
                : {}),
            ...getCustomChecksAndErrors(def, opts),
            options: def.options.map((opt, idx) => {
                const result = s(opt, {
                    ...opts,
                    currentPath: [...opts.currentPath, "options", String(idx)],
                });
                return result;
            }),
        };
    },
    intersection: (def, opts) => ({
        type: "intersection",
        ...getCustomChecksAndErrors(def, opts),
        left: s(def.left, {
            ...opts,
            currentPath: [...opts.currentPath, "left"],
        }),
        right: s(def.right, {
            ...opts,
            currentPath: [...opts.currentPath, "right"],
        }),
    }),
    promise: (def, opts) => ({
        type: "promise",
        value: s(def.innerType, {
            ...opts,
            currentPath: [...opts.currentPath, "value"],
        }),
    }),
    lazy: (def, opts) => {
        const getter = def.getter();
        return s(getter, opts, 
        // official equivalent for `isOptional`
        getter.safeParse(undefined).success ||
            // official equivalent for `isNullable`
            getter.safeParse(null).success);
    },
    transform: (def, opts) => {
        let name = null;
        if ("transforms" in opts && opts.transforms) {
            for (const [transformName, transformItem] of Object.entries(opts.transforms)) {
                if (def.type === "transform" && transformItem === def.transform) {
                    name = transformName;
                    break;
                }
            }
        }
        return {
            type: "transform",
            name,
            // inner: s(def.out, {
            //   ...opts,
            //   currentPath: [...opts.currentPath, "inner"],
            // }),
        };
    },
    pipe: (def, opts) => {
        if (!("transforms" in opts)) {
            return s(def.out, opts);
        }
        return {
            type: "pipe",
            ...getCustomChecksAndErrors(def, opts),
            inner: s(def.in, opts),
            outer: s(def.out, opts),
        };
    },
    catch: (def, opts) => {
        const catchValue = def.catchValue({
            value: null,
            issues: [],
            // No errors to report, so just add an empty set
            /* c8 ignore next 3 -- Unused */
            get error() {
                return new z.ZodError([]);
            },
            // We don't have any input yet, so just provide `undefined`
            input: undefined,
        });
        return {
            type: "catch",
            value: catchValue,
            innerType: s(def.innerType, opts),
        };
    },
    readonly: (def, opts) => ({
        ...s(def.innerType, opts, true),
        readonly: true,
    }),
};
// Must match the exported Zerialize types
export function zerializeRefs(schema, opts, wrapReferences) {
    // export function zerialize(schema: ZodTypes, opts?: Partial<ZerializerOptions> | undefined): unknown {
    if (opts.seenObjects.has(schema)) {
        return wrapReferences // && schema._def.typeName !== "ZodOptional"
            ? {
                type: "union",
                options: [{ $ref: opts.seenObjects.get(schema) }],
                ...(typeof schema.description === "string"
                    ? {
                        description: schema.description,
                    }
                    : {}),
            }
            : { $ref: opts.seenObjects.get(schema) };
    }
    const { _zod: { def }, } = schema;
    const objectPath = "#" + (opts.currentPath.length ? "/" + opts.currentPath.join("/") : "");
    opts.seenObjects.set(schema, objectPath);
    const zer = zerializers[def.type](def, opts);
    if (typeof schema.description === "string") {
        zer.description = schema.description;
    }
    return zer;
}
export function zerialize(schema, opts = {}) {
    if (!opts.currentPath) {
        opts.currentPath = [];
    }
    if (!opts.seenObjects) {
        opts.seenObjects = new WeakMap();
    }
    return zerializeRefs(schema, opts);
}
