/**
 * Convenience wrapper that negotiates the best media type in one call.
 *
 * This calls `negotiateMediaTypeFactory` to create a negotiator for the
 * provided `availableMediaTypes` and immediately invokes it with the given
 * `Accept` header.
 *
 * @param availableMediaTypes - Server-supported media type strings.
 * @param accept - The raw `Accept` header value to negotiate. If falsy,
 *   the first available media type is returned.
 * @param permissive - If `true`, parser operates in permissive mode tolerating
 *   some non-RFC-compliant inputs (extra OWS, empty parameter values,
 *   flag-like parameters, truncated quoted values).
 * @returns The best matching available media type string, or `null` if
 *   none match.
 */
declare const negotiateMediaType: (availableMediaTypes: string[], accept?: string | null | undefined, permissive?: boolean) => string | null;
export default negotiateMediaType;
//# sourceMappingURL=negotiateMediaType.d.ts.map