# Backend FastAPI server URL
VITE_API_BASE_URL=http://localhost:8000

# Gemini API Key (optional — backend uses its own key, frontend only needs this for direct calls)
GEMINI_API_KEY=MY_GEMINI_API_KEY

# APP URL (Cloud Run / production deployment URL)
APP_URL=MY_APP_URL

